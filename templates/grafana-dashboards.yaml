{{- if .Values.grafana.config.dashboards }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Release.Name }}-grafana-dashboards
  labels: {{- include "gitops-config.labels" . | nindent 4 }}
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ .Release.Name }}-grafana-dashboards
  source: {{ toYaml .Values.grafana.repository | nindent 4 }}

    helm:
      values: |- 
        {{ tpl (toYaml .Values.grafana.config) . | nindent 8 }}
        

  destination: {{ toYaml .Values.grafana.destination | nindent 4}}
  syncPolicy: {{ toYaml .Values.syncPolicy | nindent 4}}
{{- end }}